# 2026-05-18

## 完成：D1 云同步方案实施

按照计划文件 `/Users/hongyangchun/.workbuddy/plans/blazing-nebula-curie.md` 完成了全部 5 个阶段的实施：

1. **P1 基础设施**：创建 `schema.sql`（3 张表：worlds/chronicles/sync_meta）、`wrangler.toml`（D1 binding）、修改 `_redirects`
2. **P1 API**：创建 `functions/api/` 目录，包含 `_middleware.ts`（Access 认证）、`world.ts`（GET/POST）、`chronicles.ts`（GET/POST）
3. **P2 SyncService**：创建 `src/services/syncService.ts`，实现 debounce（2s）、认证检测、初始拉取合并、离线重试
4. **P3 接入**：修改 `persist()` 添加 `syncService.syncWorld()`，修改 `saveChronicles()` 添加 `syncService.syncChronicles()`
5. **P4 SyncLoader**：创建 `SyncLoader.tsx`（初始同步加载器），修改 `App.tsx` 包裹
6. **P5 Settings UI**：在数据管理 Tab 添加云同步状态卡片（认证状态、上次同步时间、手动同步按钮）

**构建验证**：`tsc --noEmit` 0 错误，`vite build` 成功（1085 kB）

**部署待办**：
- 运行 `npx wrangler d1 create zzworld-db` 创建数据库
- 将 database_id 填入 `wrangler.toml`
- 运行 `npx wrangler d1 execute zzworld-db --remote --file=./schema.sql` 创建表
- 在 Cloudflare Zero Trust 配置 Access 应用（绑定 Pages 域名）
- 部署：`npm run build && npx wrangler pages deploy dist`

## 部署 & Access 配置进展

- 用户通过 CLI 手动部署成功，获得 URL：`https://4b96d471.realmkit.pages.dev`
- Git 连接部署和 CLI 部署可共存于同一 Pages 项目，无冲突
- 更新了 `docs/deploy-guide.md` Step 5 的导航路径（Zero Trust 入口、Identity providers 路径、Applications 创建路径）
- 用户已成功配置 One-time PIN 登录
- **Google OAuth 报错 `Error 401: invalid_client`**：根因是未在 Google Cloud Console 创建 OAuth 客户端。已写入完整步骤到 deploy-guide.md（新增 5.3 节），故障排查部分也添加了对应条目。用户尚未完成修复。

## 修复：地图城市弹窗文字不可读

- **问题**：`MarkerDetailDialog` 深蓝渐变背景（`#1a237e → #283593`）上，MUI Chip（人口、坐标、人物名）和 Divider 使用默认主题色，对比度极低
- **修复**：在 `DialogContent` 内容容器上添加 MUI 样式覆盖，Chip 文字/边框改为白色系，Divider 改为半透明白色
- 已提交推送：`0d2a813`（feat: D1 云同步 + 备份追踪 + 导入导出增强 + 地图弹窗样式修复）

## 重新部署

- **根因**：项目使用 CLI 手动部署（`npx wrangler pages deploy dist`），GitHub push 不会触发自动部署
- 顺带修复 `FactionForm.tsx` 构建错误（`PRESET_COLORS` 使用 `as const` 导致 color state 类型过严，改为 `useState<string>`）
- 提交：`8c1c00f`，构建 + 部署成功 → `https://9a557e76.realmkit.pages.dev`
