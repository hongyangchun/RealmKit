# 2026-06-13

## CharacterCard 三国杀风格改造

- 将 `src/components/character/CharacterCard.tsx` 全面改造为三国杀武将卡牌风格
  - 深色羊皮纸底色（`#1a100a`）
  - 势力色描金边框（2px solid factionColor）
  - 四角 L 纹金色装饰（CornerBrackets 组件）
  - 标题栏：势力色渐变底 + 金色描边 + 白金字
  - 立绘区：暗色底 + 方形 Avatar（borderRadius: 4px）+ 径向晕染
  - 技能/特质分区用菱形分割线（GoldDivider）
  - 特质 Chip：印章风深底金色描边
  - 传记文字：古金色（#a89070）
  - 悬停：浮起 + 金色边框脉冲动画
- 同步改造 `src/components/character/SkillChip.tsx`
  - 改为自定义 Box 组件（替代 MUI Chip）
  - 三色体系：主动赤 / 被动青 / 特殊紫
  - 深底色半透明背景 + 彩色描边

## 编辑态隐藏人物总评 + 技能描述字段

- `CharacterDetailPanel.tsx`：新增 `isEditing` prop（可选），为 true 时隐藏「人物总评」模块（避免自动生成的总评内容干扰用户编辑）
- `CharacterPage.tsx`：在挂载 `<CharacterDetailPanel>` 处传 `isEditing={!!editingChar}`，与编辑弹窗状态联动
- `CharacterForm.tsx`：技能行从单行布局改为两行（名称+类型 一行 / 描述 一行），新增「技能描述」多行 TextField，绑定到 `skill.description` 字段（Skill 类型原本就有 description，无需改类型）
  - 技能行加浅米色虚线边框分组，提升可读性
  - 描述输入框 multiline + minRows=1 + maxRows=4，自适应高度

## 三国杀卡片设计全面分析 & v2.0 提案

- 编写 `docs/character-card-redesign.md` — 完整的三国杀风格人物卡片重设计方案
  - **12区解剖模型**：将三国杀武将牌分解为 Z1-Z12 完整区域定义
  - **差距分析矩阵**：8项关键差距，4项 P0 级别（武将名、插画区、勾玉体力、技能完整描述）
  - **数据模型扩展**：新增 5 字段 — hp(体力值)、nickname(称号)、portrait(立绘)、cardNumber(编号)、rarity(稀有度)
  - **勾玉设计**：SVG Path 实现阴阳鱼/勾玉形状，力荐方案A
  - **技能展示重做**：从紧凑 Chip → 完整描述块（SkillBlock），z-index 分3层
  - **稀有度体系**：4级（common/rare/epic/legendary），自动计算为主+手动覆盖
  - **5阶段实施路线图**：Phase1数据模型 → Phase2卡片重做 → Phase3导出 → Phase4稀有度 → Phase5打印
- 创建 2 张 SVG 可视化图表：12区解剖热力图 + 新旧卡片对比图

## Phase 1 实施：数据模型 + 组件重构

- **数据模型升级** (`src/types/index.ts`)
  - 新增 `CardRarity` 类型 (common/rare/epic/legendary)
  - Character 接口新增 5 字段: hp, nickname, portrait, cardNumber, rarity
- **CharacterForm 更新** (`src/components/character/CharacterForm.tsx`)
  - 添加称号输入框、体力值 Slider(1-8)、稀有度 Select、立绘URL、卡牌编号
  - 新增状态变量 + dirtyCheck 包装 + useEffect 初始化 + handleSubmit 保存
- **勾玉组件** (`src/components/character/JadeTokens.tsx`)
  - 阴阳鱼造型体力标记（径向渐变CSS实现）
  - 支持 count(1-8)、size、color 配置
- **CharacterCard v2.0 重做** (`src/components/character/CharacterCard.tsx`)
  - 5:7 aspectRatio 三国杀竖版比例
  - Z1-Z2: 称号「」+ 姓名 1.3rem 居中
  - Z3-Z5: 260px 插画区 (支持 portrait 立绘 URL) + 编号+稀有度角标
  - Z6: 勾玉体力 (JadeTokens size=22)
  - Z7-Z10: 技能完整描述块 (替代 SkillChip Chip)
  - Z11-Z12: 底部信息栏 (寿命+势力)
- **备份**: CharacterCard.sanguosha.tsx (新组件副本)
- 编译通过 (tsc --noEmit 0 errors)

## Phase 3-4: 导出对齐 + 稀有度体系

- **CharacterCard.tsx 修复**
  - 插画区 80px → 260px，支持 portrait 立绘 + avatar 回退
  - JadeTokens 勾玉正式渲染（之前导入但未使用）
  - 稀有度角标 + 卡牌编号叠加在插画区角落
- **CharacterCardExporter.tsx 全字段重写**
  - 导入 CardRarity 类型 + RARITY_CN 配色表
  - 立绘区: 224px canvas 高度，portrait/avatar 图片绘制 + cover 裁剪 + 暗角晕染
  - 勾玉体力: drawJade() 径向渐变 Canvas 绘制
  - 称号 nickname 集成到顶部色带
  - 稀有度内框高亮替代原金色内框
  - 技能描述 max 2 行（原 1 行）
  - 底栏: 势力·称号「」| 稀有度·编号
  - calcCardHeight 重算适配新字段
- **src/utils/rarity.ts 新建**
  - calcAutoRarity(): 基于技能数/传记长度/事件关联自动判定
  - calcRarityForCharacter(): Character 对象直接计算
  - 规则: common<rare(技能≥4)<epic(技能≥5+bio≥100字)<legendary(技能≥6+bio≥200+事件≥5)
- **CharacterForm** 集成稀有度推荐: Select 下方显示"系统推荐: XXX"提示（颜色不匹配时）
- 编译验证: npx tsc --noEmit → 0 errors

## Phase 5: 打印功能

- **`src/components/character/CharacterCardPrint.tsx`**（新建）
  - 纯 CSS 渲染打印卡片，物理尺寸 63×88mm（三国杀标准扑克牌尺寸）
  - 12 区布局匹配 CharacterCard.tsx v2.0（称号/插画/勾玉/技能/底栏）
  - 2mm 出血区 + 四角裁切线，mm 单位精确排版
- **`src/components/character/CharacterCardPrintSheet.tsx`**（新建）
  - A4 纸 3列×3行 = 9 张卡片/页（210×297mm）
  - 布局公式：3×63 + 2×5(gap) + 2×5.5(margin) = 210mm ✓
  - 导出 `printCharacterCards()` 工具函数：动态注入 body 级打印容器 + @media print CSS + afterprint 清理（含 60s 超时回退）
  - `CharacterCardPrintContainer` 组件：支持预览/打印双模式
- **`src/components/character/PrintPreviewDialog.tsx`**（新建）
  - MUI Dialog 打印预览：缩放 Slider (20%-150%)、全屏切换、卡片/页数统计
  - 打印按钮调用 `printCharacterCards()`
- **`src/pages/CharacterPage.tsx`**（修改）
  - 工具栏新增"打印卡片"按钮（卡片视图模式，禁用条件：无过滤结果）
  - 新增 `printData` memo（filteredCharacters + factions 映射）
  - 集成 PrintPreviewDialog

构建状态：`tsc --noEmit` 0 errors，`vite build` 通过

## 导出器 v4.0 逐像素对齐预览版

- **`src/components/character/CharacterCardExporter.tsx`** 全面重写（v3→v4），逐项对齐 CharacterCard.tsx 预览：
  - ✅ 修复双#号 bug（`#${fc}cc` → `${fc}cc`），border 颜色正常渲染
  - ✅ 字体族统一为 `"LXGW WenKai TC","Noto Serif SC",serif`（含 sans-serif Chip 标签）
  - ✅ 字体大小精确 2x 缩放：rem×16×2（如 1.3rem→42px, 0.7rem→22px）
  - ✅ 头部渐变改为 135deg 对角渐变（匹配预览 linear-gradient(135deg)）
  - ✅ 添加斜纹纹理叠加层（Canvas createPattern + rotate -55deg）
  - ✅ 添加顶部内高光（inset 0 1px 0 rgba(255,255,255,0.04)）
  - ✅ 技能类型标签改为 Chip 风格（圆角背景+势力色，非纯文本）
  - ✅ 寿命「今」→「至今」
  - ✅ 无图回退改为圆角方框 Avatar（匹配预览 100×100→200×200 + 边框 + 阴影 + 首字）
  - ✅ 称号移除斜体
  - ✅ 添加 letterSpacing 模拟（逐字绘制 + 间距控制）
  - ✅ 勾玉颜色对齐 JadeTokens.tsx：#DAA520/#8B6914（非 #ddc274/#6B4914）
  - ✅ 四角 L 纹 lineCap 改为 butt（匹配预览）

## CharacterDetailPanel v2.0 — 显示内容与编辑表单对齐

- **`src/components/character/CharacterDetailPanel.tsx`** v2.0 重写，新增 6 项 CharacterForm 中已存在但详情面板缺失的字段显示：
  1. **称号 (nickname)** — 姓名上方以「称号」格式显示，势力色
  2. **体力 (hp)** — JadeTokens 小号勾玉可视化（size=14）+ ❤️ 图标
  3. **稀有度 (rarity)** — 四色 Chip 角标（灰/蓝/紫/金）+ 自动推荐不匹配提示
  4. **立绘 (portrait)** — 若有 portrait URL 则在顶部显示缩略图（maxH:220, cover）
  5. **卡牌编号 (cardNumber)** — monospace 字体显示 `#WEI 001` 格式
  6. **技能描述** — 从 SkillChip 紧凑标签升级为完整卡片（技能名+类型Chip+描述全文）
- 技能区域改为独立卡片布局，含类型图标（⚡主动/🛡被动/⭐特殊）+ 边框色区分
- 稀有度字段添加 `calcRarityForCharacter` 自动推荐与当前值比对
- 原有模块完全保留：关系/事件/总评/情节建议/编辑/删除按钮
- 构建状态：`tsc --noEmit` 0 errors，`vite build` 通过

## 清理：删除人物卡片页面"剧情灵感"模块

- 从 `CharacterDetailPanel.tsx` 移除：
  - `PlotSuggestionPanel` 组件及 import
  - `handleCreateEvent` 回调（仅用于 PlotSuggestionPanel）
  - `addEvent` / `updateCharacter` store 方法（仅用于 handleCreateEvent）
  - `eventSnackbar` 状态 + `Snackbar` / `Alert` MUI 组件（仅用于 handleCreateEvent）
  - `HistoryEvent` 类型 import
- `tsc --noEmit` → 0 errors

## 卡片包功能 — 完整实现

- **`src/types/index.ts`** 新增 `CardPack` 接口：
  - `id / name / description / characterIds[] / createdAt / updatedAt`
  - `WorldData` 新增 `cardPacks: CardPack[]` 字段
- **`src/store/worldStore.ts`** 新增 5 个 actions：
  - `addCardPack(name, description)` — 新建包
  - `updateCardPack(id, patch)` — 编辑名称/描述
  - `deleteCardPack(id)` — **仅删除包，不级联删除人物**
  - `addCharacterToPack(packId, charId)` — 去重添加
  - `removeCharacterFromPack(packId, charId)` — 从包移除
  - `DEFAULT_WORLD_DATA.cardPacks = []` 初始化
  - `applyWorldSeed` 清空卡片包（旧 ID 失效）
- `src/components/character/CardPackPanel.tsx`（新建）：
  - 网格列表卡片（名称/描述/人物头像预览/创建日期）
  - 新建/编辑 Dialog（名称必填 + 描述可选）
  - 包详情 Dialog：Autocomplete 搜索添加 + 人物缩略网格 + 移除按钮
  - 删除确认对话框（明确告知不删人物）
  - 回退 EmptyState 空状态引导
- `CardPackPage.tsx` 已废弃（待删），改为 `CardPackPanel` 内嵌组件
- 构建状态：`tsc --noEmit` 0 errors，`vite build` 通过
- **2026-06-13: 卡片包入口整合到人物页面**
  - 入口从独立页面/侧栏迁移到 CharacterPage，作为第三视图模式与「网络图」「卡片」并列
  - **CharacterPage.tsx** 变更：
    - `ViewMode` 扩展为 `'graph' | 'cards' | 'packs'`
    - 新增 `handleViewModeChange`：切换到 packs 时自动清除人物选中态（detail panel 不适用）
    - ToggleButtonGroup 新增 `<ToggleButton value="packs">` (StyleIcon + "卡片包")
    - 搜索栏：仅 graph/cards 模式下显示 (`viewMode !== 'packs'`)
    - "新建人物" 按钮：仅 graph/cards 模式下显示
    - 人物计数：packs 模式显示 `{N} 个卡片包`
    - 主内容区：新增 `viewMode === 'packs'` 分支渲染 `<CardPackPanel />`
    - 桌面 Detail Panel / 移动端 Detail Drawer：packs 模式下均隐藏
  - **App.tsx**：移除 `/cardpacks` 路由和 `CardPackPage` import
  - **Sidebar.tsx**：移除「卡片包·英灵编队」导航项
  - 构建状态：`tsc --noEmit` 0 errors，`vite build` 通过
